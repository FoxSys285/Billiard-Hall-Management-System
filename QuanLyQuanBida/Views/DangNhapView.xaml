<UserControl x:Class="QuanLyQuanBida.Views.DangNhapView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Loaded="UserControl_Loaded">

    <Grid>

        <!-- Nền -->
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#0B3D2E" Offset="0"/>
                <GradientStop Color="#146C43" Offset="0.5"/>
                <GradientStop Color="#1E8449" Offset="1"/>
            </LinearGradientBrush>
        </Grid.Background>

        <!-- Hiệu ứng nền -->
        <Ellipse Width="600"
                 Height="600"
                 Fill="#20FFFFFF"
                 HorizontalAlignment="Left"
                 VerticalAlignment="Top"
                 Margin="-200,-200,0,0"/>

        <Ellipse Width="500"
                 Height="500"
                 Fill="#10FFFFFF"
                 HorizontalAlignment="Right"
                 VerticalAlignment="Bottom"
                 Margin="0,0,-150,-150"/>

        <!-- ViewBox giúp không bị cắt giao diện -->
        <Viewbox Stretch="Uniform">

            <Border Width="450"
                    Height="550"
                    Background="#102A20"
                    CornerRadius="25"
                    Padding="35">

                <Border.Effect>
                    <DropShadowEffect BlurRadius="25"
                                      ShadowDepth="6"
                                      Opacity="0.4"/>
                </Border.Effect>

                <StackPanel VerticalAlignment="Center">

                    <!-- Logo -->
                    <TextBlock Text="🎱"
                               FontSize="70"
                               HorizontalAlignment="Center"/>

                    <TextBlock Text="BILLIARD CLUB"
                               Foreground="#58D68D"
                               FontSize="30"
                               FontWeight="Bold"
                               HorizontalAlignment="Center"/>

                    <TextBlock Text="QUẢN LÝ QUÁN BIDA"
                               Foreground="White"
                               FontSize="16"
                               Margin="0,5,0,35"
                               HorizontalAlignment="Center"/>

                    <!-- Tài khoản -->
                    <TextBlock Text="Tài khoản"
                               Foreground="White"
                               FontWeight="SemiBold"/>

                    <Border Background="#1B4332"
                            CornerRadius="10"
                            Margin="0,8,0,20">

                        <TextBox Height="45"
                                 BorderThickness="0"
                                 Background="Transparent"
                                 Foreground="White"
                                 VerticalContentAlignment="Center"
                                 FontSize="16"
                                 Padding="15,0"
                                 Text="{Binding InputTK,
                                 UpdateSourceTrigger=PropertyChanged}"/>
                    </Border>

                    <!-- Mật khẩu -->
                    <TextBlock Text="Mật khẩu"
                               Foreground="White"
                               FontWeight="SemiBold"/>

                    <Border Background="#1B4332"
                            CornerRadius="10"
                            Margin="0,8,0,30">

                        <PasswordBox x:Name="PasswordBox"
                                     Height="45"
                                     BorderThickness="0"
                                     VerticalContentAlignment="Center"
                                     Background="Transparent"
                                     Foreground="White"
                                     FontSize="16"
                                     Padding="15,0"
                                     PasswordChanged="PasswordBox_PasswordChanged"/>
                    </Border>

                    <!-- Button -->
                    <Button Content="ĐĂNG NHẬP"
                            Height="50"
                            FontSize="18"
                            FontWeight="Bold"
                            Cursor="Hand"
                            Command="{Binding DangNhapCommand}"
                            Background="#2ECC71"
                            Foreground="White"
                            BorderThickness="0">

                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border x:Name="bd"
                                                    Background="{TemplateBinding Background}"
                                                    CornerRadius="12">
                                                <ContentPresenter
                                                    HorizontalAlignment="Center"
                                                    VerticalAlignment="Center"/>
                                            </Border>

                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver"
                                                         Value="True">
                                                    <Setter TargetName="bd"
                                                            Property="Opacity"
                                                            Value="0.85"/>
                                                </Trigger>

                                                <Trigger Property="IsPressed"
                                                         Value="True">
                                                    <Setter TargetName="bd"
                                                            Property="Opacity"
                                                            Value="0.7"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </Button.Style>

                    </Button>

                    <TextBlock Text="© Billiard Management System"
                               Foreground="#AAAAAA"
                               FontSize="12"
                               Margin="0,30,0,0"
                               HorizontalAlignment="Center"/>

                </StackPanel>

            </Border>

        </Viewbox>

    </Grid>
</UserControl>