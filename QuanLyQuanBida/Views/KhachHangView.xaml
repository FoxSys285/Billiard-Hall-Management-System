<UserControl x:Class="QuanLyQuanBida.Views.KhachHangView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:QuanLyQuanBida.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="734" d:DesignWidth="1150"
             d:DataContext="{d:DesignInstance local:MainViewModel, IsDesignTimeCreatable=True}">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>

        <Style TargetType="Button" x:Key="ActionButtonStyle">
            <Setter Property="Background" Value="#4e6e5d"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Resources>
                <Style TargetType="Border">
                    <Setter Property="CornerRadius" Value="5"/>
                </Style>
            </Style.Resources>
        </Style>

        <Style TargetType="TextBox" x:Key="ModernTextBox">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Padding" Value="0,5,0,5"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
    </UserControl.Resources>

    <Grid Background="#FFE9E9E9">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="120"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border BorderBrush="#FFF2EDC2" BorderThickness="1" Margin="30,20,30,0" Background="White" CornerRadius="20">
                <Grid>
                    <TextBlock HorizontalAlignment="Left" Text="Khách Hàng" VerticalAlignment="Center" FontWeight="Bold" FontSize="36" Foreground="Black" Margin="40,0,0,0"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,40,0">
                        <TextBlock Text="{Binding CurrentAccount.TenNV}" FontWeight="Bold" FontSize="20" VerticalAlignment="Center" Margin="0,0,20,0"/>
                        <Button Content="❌" Background="{x:Null}" FontSize="30" Foreground="Black" BorderBrush="{x:Null}" Click="Button_Click" Cursor="Hand"/>
                    </StackPanel>
                </Grid>
            </Border>

            <Border BorderBrush="#FFF2EDC2" BorderThickness="1" Grid.Row="1" Margin="30,20,30,20" Background="White" CornerRadius="20">
                <Grid Margin="40,30">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0" Margin="0,0,0,30">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" VerticalAlignment="Bottom">
                            <TextBlock Text="Tìm kiếm theo SĐT" Foreground="#AAAAAA" FontSize="14" Margin="0,0,0,5"/>
                            <TextBox Style="{StaticResource ModernTextBox}" 
                                     Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                                     Width="450" HorizontalAlignment="Left"/>
                        </StackPanel>

                        <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Bottom">
                            <Button Content="Thêm" Style="{StaticResource ActionButtonStyle}" 
                                    Width="180" Height="45" Margin="0,0,20,0"
                                    Command="{Binding OpenAddCommand}"/>
                            <Button Content="Xóa" Style="{StaticResource ActionButtonStyle}" 
                                    Width="180" Height="45"
                                    Command="{Binding DeleteKhachHangCommand}"/>
                        </StackPanel>
                    </Grid>

                    <DataGrid Grid.Row="1" ItemsSource="{Binding DsKhachHang}" SelectedItem="{Binding SelectedKhachHang}"
                              AutoGenerateColumns="False" IsReadOnly="True" Background="Transparent" BorderThickness="0" 
                              GridLinesVisibility="None" RowHeight="50" FontSize="16" CanUserAddRows="False">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Mã khách hàng" Binding="{Binding MaKhachHang}" Width="180"/>
                            <DataGridTextColumn Header="Họ tên" Binding="{Binding TenKhachHang}" Width="*"/>
                            <DataGridTextColumn Header="SĐT" Binding="{Binding SoDienThoai}" Width="300"/>
                            <DataGridTextColumn Header="Số điểm" Binding="{Binding DiemTichLuy}" Width="120"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>
        </Grid>

        <Grid Background="#55000000" 
              Visibility="{Binding IsShowAdd, Converter={StaticResource BooleanToVisibilityConverter}}">

            <Border Background="White" CornerRadius="15" Width="450" 
                    VerticalAlignment="Center" HorizontalAlignment="Center" Padding="30">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0" Margin="0,0,0,30">
                        <TextBlock Text="KHÁCH HÀNG MỚI" FontWeight="Bold" FontSize="18" Foreground="#666666" VerticalAlignment="Center"/>
                        <Button Content="✕" Command="{Binding CloseAddCommand}" HorizontalAlignment="Right" 
                                Background="Transparent" BorderThickness="0" FontSize="20" Cursor="Hand" Foreground="#666666"/>
                    </Grid>

                    <StackPanel Grid.Row="1" Margin="0,0,0,30">
                        <TextBlock Text="Mã khách hàng" Foreground="#AAAAAA" FontSize="13" Margin="0,0,0,5"/>
                        <TextBox Text="{Binding AddMaKH}" Style="{StaticResource ModernTextBox}" IsReadOnly="True" Foreground="#888888" Margin="0,0,0,20"/>

                        <TextBlock Text="Tên khách hàng" Foreground="#AAAAAA" FontSize="13" Margin="0,0,0,5"/>
                        <TextBox Text="{Binding AddTenKH, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernTextBox}" Margin="0,0,0,20"/>

                        <TextBlock Text="Số điện thoại" Foreground="#AAAAAA" FontSize="13" Margin="0,0,0,5"/>
                        <TextBox Text="{Binding AddSDT, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernTextBox}"/>
                    </StackPanel>

                    <Button Grid.Row="2" Content="THÊM KHÁCH HÀNG" Command="{Binding ConfirmAddCommand}"
                            Style="{StaticResource ActionButtonStyle}" Height="50" FontSize="18"/>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>